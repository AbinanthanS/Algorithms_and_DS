#include<bits/stdc++.h>
using namespace std;


int main(){
	vector<int> v = {1,2,3};
/*	
	//vector<int>::iterator it = v.begin(); //cout<<*(it+1);
	for(auto it = v.begin();it!= v.end();it++){
		cout<<*it<<endl;
	}
*/
/*	vector<pair<int,int>> v_p = {{1,2},{2,3},{3,4}};
	//vector<pair<int,int>>::iterator it;
	for (auto it = v_p.begin();it != v_p.end();it++){
	    cout<<it->first<<" "<<it->second<<endl;  // it->first <=> (*it).first && it->second<=> (*it).second
    }
*/
    //vector<pair<int,int>> v_p = {{1,2},{2,3},{3,4}};
    //for(auto &val:v_p) cout<<val.first<<" "<<val.second<<endl;   // for(pair<int,int> &val:v_p)


*/
} 
